export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7288/api',
  /** Local-dev fallback only when no school is stored on device. Leave empty to always use school-code screen. */
  tenantSubdomain: '',
};
