import { Component } from '@angular/core';

/**
 * SmartOps UI list-page-shell equivalent for Ionic modules.
 * Wraps list header, toolbar, and body in a consistent flex column layout.
 */
@Component({
  selector: 'so-module-shell',
  templateUrl: './so-module-shell.component.html',
  styleUrls: ['./so-module-shell.component.scss'],
})
export class SoModuleShellComponent {}
